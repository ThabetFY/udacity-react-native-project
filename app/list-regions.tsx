import React from "react";
import { SafeAreaView, ScrollView, Text, View } from "react-native";

export default function ListRegions() {
  return (
    <SafeAreaView>
      <ScrollView>
        <View>
          <Text>This is the ListRegions screen</Text>
        </View>
      </ScrollView>
    </SafeAreaView>
  );
}
